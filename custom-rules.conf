# Detect simple SQLi patterns
SecRule ARGS "union.*select" "id:1001,phase:2,deny,log,msg:'SQL Injection detected'"

# Detect simple XSS patterns
SecRule ARGS "<script>" "id:1002,phase:2,deny,log,msg:'XSS attack detected'"
