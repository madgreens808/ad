# Simple, syntactically correct rules for testing (detection-only)
SecRule REQUEST_HEADERS:User-Agent "@pm sqlmap nikto w3af" "id:950102,phase:1,pass,log,msg:'scanner UA detected'"

# Detect basic UNION SELECT in arguments (detection only)
SecRule ARGS|ARGS_NAMES|REQUEST_BODY "@rx (?i)union\s+select" "id:950100,phase:2,pass,log,msg:'vulnapp SQLi candidate',t:none"

# Detect simple reflected XSS script tag (detection only)
SecRule ARGS|ARGS_NAMES "@rx <script" "id:950101,phase:2,pass,log,msg:'vulnapp reflected XSS candidate'"
